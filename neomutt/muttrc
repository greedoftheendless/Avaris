# vim: filetype=muttrc

#Source color index
source ./colors

#Source accounts first (sets $folder for mailboxes)
source ./accounts/alt-acc

#Source settings
source ./settings

#Source the keybinds
source ./keybinds

folder-hook $folder 'source ./accounts/alt-acc'
